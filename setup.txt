# RL-branch is an extension project of WiseRL, which adds optional distributed training capabilities and enriches the requirements for RL algorithms.
#! /usr/bin/env python
# -*- coding: utf-8 -*-
try:
    from setuptools import setup,find_packages
except ImportError:
    from distutils.core import setup
import setuptools

setup(
    name='wiserl',  # package name
    author='xueqian bai',  # author
    version='0.1.0',  # version
    license='MIT',

    description='RL-branch stands as a distributed reinforcement learning framework, which accomplishes the implementation of distributed reinforcement learning algorithms by amalgamating distributed data sampling and centralized training. ',
    author_email = 'baicrystal25@gmail.com',
    url='https://github.com/Hakuna25',
    # package dependence
    install_requires=[
        'ray',
        "gym"
    ],
    packages=find_packages(),
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
)